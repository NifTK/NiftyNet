[settings]
cuda_devices = ""
model_dir = ./models/model_toy
net_name = toynet

# preprocessing threads parameters
queue_length = 20
num_threads = 2

# network parameters (controlling memory consumption)
batch_size = 1
image_size = 32
label_size = 32
num_classes = 5

# affected by network's receptive field
volume_padding_size = 21

# ** inputs regarding the normalisation and preceding masking

flag_normalisation = False
flag_whitening = True
flag_spatial_scaling = True
flag_saving_norm = True

histogram_ref_file = ./example_volumes/multimodal_BRATS/standardisation_models.txt
norm_type = percentile
flag_saving_mask = False
multimod_mask_type = and
mask_type = otsu_plus
flag_rotation = True

# ** input regarding orientation and isotropy
flag_orientation = True
flag_isotropic = True




# ** training only ** system parameters
num_gpus = 2
sample_per_volume = 100

# **training only** gradient descent and loss parameters
lr = 0.01
decay = 1e-8
loss_type = Dice
reg_type = l2
starting_iter = 0
save_every_n = 100
max_iter = 10

# **inference only** system parameters
border = 5
pred_iter = 10
save_seg_dir = ./output/model_multimod1_output
eval_data_dir = ./example_volumes/multimodal_BRATS

train_data_dir = ./testing_data

# ** sampling strategy to adopt
compulsory_labels = 0
min_sampling_ratio = 0.001
min_numb_labels = 1
